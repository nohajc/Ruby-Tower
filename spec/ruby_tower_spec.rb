require 'spec_helper'

describe RubyTower do
  it 'has a version number' do
    expect(RubyTower::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
